version: '2'
services:
  sofftekexam-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/sofftekExam/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=sofftekExam
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
